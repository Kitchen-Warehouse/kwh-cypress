name: Cypress Tests
on: 
  push:
  repository_dispatch:
    types: [run-cypress-tests]

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    # Runs tests in parallel with matrix strategy https://docs.cypress.io/guides/guides/parallelization
    # https://docs.github.com/en/actions/using-jobs/using-a-matrix-for-your-jobs
    # Also see warning here https://github.com/cypress-io/github-action#parallel
    strategy:
      fail-fast: false # https://github.com/cypress-io/github-action/issues/48
      matrix:
        containers: [1, 2] # Uses 2 parallel instances
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Set preview URL
        run: |
          if [ "${{ github.event_name }}" = "repository_dispatch" ]; then
            # Sanitize the preview URL to remove any leading/trailing whitespace
            SANITIZED_URL=$(echo "${{ github.event.client_payload.preview_url }}" | xargs)
            echo "PREVIEW_URL=$SANITIZED_URL" >> $GITHUB_ENV
            echo "PR_NUMBER=${{ github.event.client_payload.pr_number }}" >> $GITHUB_ENV
            echo "REPO_NAME=${{ github.event.client_payload.repo_name }}" >> $GITHUB_ENV
            echo "BRANCH_NAME=${{ github.event.client_payload.branch }}" >> $GITHUB_ENV
            echo "PACKAGE_PATH=${{ github.event.client_payload.package_path }}" >> $GITHUB_ENV
            echo "🚀 Running tests for Kitchen Warehouse Frontend PR #${{ github.event.client_payload.pr_number }}"
            echo "📍 Preview URL: $SANITIZED_URL"
            echo "🌿 Branch: ${{ github.event.client_payload.branch }}"
            echo "📦 Package: ${{ github.event.client_payload.package_path }}"
          else
            echo "PREVIEW_URL=https://staging.kitchenwarehouse.com.au/" >> $GITHUB_ENV
            echo "🔄 Running tests against staging URL (regular push event)"
          fi

      - name: Cypress run
        # Uses the official Cypress GitHub action https://github.com/cypress-io/github-action
        uses: cypress-io/github-action@v6
        with:
          # Records to Cypress Cloud 
          # https://docs.cypress.io/guides/cloud/projects#Set-up-a-project-to-record
          record: true
          parallel: true # Runs test in parallel using settings above
        env:
          # Pass the preview URL to Cypress
          CYPRESS_BASE_URL: ${{ env.PREVIEW_URL }}
          CYPRESS_PR_NUMBER: ${{ env.PR_NUMBER }}
          CYPRESS_REPO_NAME: ${{ env.REPO_NAME }}
          # For recording and parallelization to work you must set your CYPRESS_RECORD_KEY
          # in GitHub repo → Settings → Secrets → Actions
          CYPRESS_RECORD_KEY: ${{ secrets.CYPRESS_RECORD_KEY }}
          # Creating a token https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/creating-a-personal-access-token
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}